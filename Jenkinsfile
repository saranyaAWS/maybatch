pipeline{
  agent any 
 stages {
  stage('Build') {
    steps {
      echo "Building" // One or more steps need to be included within the steps block.
    }
  }

  stage('Test') {
    steps {
      echo "TESTING" // One or more steps need to be included within the steps block.
    }
  }

  stage('Deploy') {
    steps {
     echo "DEPLOYING" // One or more steps need to be included within the steps block.
    }
  }

}
}
