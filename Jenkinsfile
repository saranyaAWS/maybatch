pipeline{
  agent any 
 stages {
  stage('Build') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

  stage('Test') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

  stage('Deploy') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

}
}
